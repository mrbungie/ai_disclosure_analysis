NVIDIA_API_KEY=
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=meta/llama-3.3-70b-instruct

# Independent LLM judge used by val_01_sample_and_label.py (pydantic_ai, OpenAI-compatible).
# Keep this separate from the NVIDIA_* pipeline classifier above so the judge isn't
# grading its own homework.
LLM_JUDGE_API_KEY=
LLM_JUDGE_BASE_URL=https://api.openai.com/v1
LLM_JUDGE_MODEL=gpt-4o-mini
